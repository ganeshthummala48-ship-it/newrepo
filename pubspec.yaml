name: farmer_ai
description: Farmer AI application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  cupertino_icons: ^1.0.8
  lottie: ^3.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  google_fonts: ^8.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/
    - assets/crops/
